services:
  backend:
    volumes:
      - ./tests/e2e:/fixtures:ro
    environment:
      PYTHONPATH: /app:/fixtures
    command: ["uvicorn", "mock_app:app", "--host", "0.0.0.0", "--port", "8000", "--no-access-log"]
